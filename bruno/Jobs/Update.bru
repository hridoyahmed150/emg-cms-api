meta {
  name: Update Job
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/api/v1/jobs/{{jobId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "title": "Lead Developer",
    "status": "active"
  }
}
